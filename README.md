# SSHpy
## Use SSH in Python interactive environment

- pip install paramiko
- change the IP, UserName, PassWord in SSHpy002.py
- run SSHpy002.py in Python interactive environment.
- Then you can send SSH orders and get returned informations in  Python interactive environment. 

